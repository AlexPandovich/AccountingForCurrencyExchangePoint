import BigNumber from "bignumber.js";

export default class MonthCalculations {
	dt2412Sum:BigNumber = new BigNumber(0);
	ct2412Sum:BigNumber = new BigNumber(0);
	buyCtTotal:BigNumber = new BigNumber(0);
}